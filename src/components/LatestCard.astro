---
import { Image } from "astro:assets";
import type { CollectionEntry } from "astro:content";

type Props = {
  plugins: {
    // date: string,
    name: string;
    // category: string;
    // link: string;
  };
};

const { plugins } = Astro.props;

// const categoryDisplay = categories[0].replace(/-/g, " ");
---

<a
    href=""
    class="relative flex gap-6 items-center group overflow-hidden p-4 rounded-lg bg-zinc-100 border border-zinc-200 w-full cursor-pointer hover:border-zinc-300 hover:bg-opacity-80"
  >
    <div class="w-full flex gap-6 items-center">
      <div class="flex gap-2 items-center justify-between">
        <!-- <div
          class="flex items-center justify-center border border-zinc-200 rounded-md w-16 h-16"
        >
          <Image src={beehiivIcon} alt={`icon`} class="rounded" />
        </div> -->
      </div>
      <div class="flex flex-col gap-0 w-full">
        <div class="flex items-center gap-2">
          <h3 class="text-zinc-950 font-bold leading-8 truncate text-ellipsis">
            {plugins.name}
          </h3>
        </div>
        <p class="text-zinc-700 text-sm leading-7 p-0">
          <!-- {plugin.category} -->
        </p>
      </div>
    </div>
    <div class="w-fit inline-block border border-zinc-200 rounded px-4 py-2 font-semibold text-sm"> 
      View
    </div>
  </a>

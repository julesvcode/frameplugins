---
import type { CollectionEntry } from "astro:content";
import ToolCard from "./ToolCard.astro";

type Props = {
  plugins: CollectionEntry<"plugins">;
};

const { plugins } = Astro.props;
---

<ToolCard plugins={plugins} />
